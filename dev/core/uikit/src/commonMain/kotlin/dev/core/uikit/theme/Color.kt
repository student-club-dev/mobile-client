package dev.core.uikit.theme

import androidx.compose.ui.graphics.Color

/**
 * Student Super App — brend palitrasi (auth dizayn tokenlari asosida).
 * Yorug' va qorong'i rejim uchun alohida qiymatlar.
 */

// ---- Brand — StudentClub ko'k (dizayn tokenlari: brand/brandDark/brandLight) ----
val Primary = Color(0xFF00ADEE)
val PrimaryGradientEnd = Color(0xFF0091D8)
val PrimaryAccent = Color(0xFF23C3F5)
val PrimaryDark = Color(0xFF23C3F5)
val LinkDark = Color(0xFF7FD9F7)

// ---- Ink / text (light) ----
val Ink = Color(0xFF0F2A43)
val InkMuted = Color(0xFF6B7A8D)
val InkFaint = Color(0xFF8A98A8)
val LabelInk = Color(0xFF6B7A8D)

// ---- Text (dark) ----
val TextDark = Color(0xFFEAF3FA)
val TextMutedDark = Color(0xFF9DAFBF)
val TextFaintDark = Color(0xFF8394A5)

// ---- Surfaces ----
val SurfaceLight = Color(0xFFEEF3F8)
val SurfaceDark = Color(0xFF0B1622)

// ---- Background gradient stops (light) ----
val BgTopLight = Color(0xFFE7F7FD)
val BgMidLight = Color(0xFFEEF3F8)
val BgBottomLight = Color(0xFFEAF0F6)

// ---- Background gradient stops (dark) ----
val BgTopDark = Color(0xFF0E1C2A)
val BgMidDark = Color(0xFF0B1622)
val BgBottomDark = Color(0xFF0C1826)

// ---- Feedback ----
val Success = Color(0xFF16B364)
val SuccessDeep = Color(0xFF16B364)

// ---- Module accents (rol / success kartalar) ----
val ModuleFood = Color(0xFFF0842B)
val ModuleStudy = Color(0xFF00ADEE)
val ModuleEmployer = Color(0xFFE0A020)
val ModuleHousing = Color(0xFF16B364)
val ModuleMedical = Color(0xFFF0567C)
